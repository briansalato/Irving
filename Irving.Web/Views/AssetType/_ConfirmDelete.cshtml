@model Irving.Web.Models.AssetType

<div id="confirmDelete" class="confirmDelete hidden">
    <form action="@(Url.DeletePost<AssetType>())" method="post">
        @Html.HiddenFor(m => m.Id)

        <p>Are you sure you want to delete this asset type? This change cannot be reverted.</p>

        <input type="submit" value="Delete" />
        <input type="button" onclick="Irving.DbCRUD.hideConfirmDelete()" value="Cancel" />
    </form>
</div>